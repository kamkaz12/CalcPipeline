node {
    stage('Connect to GIT') {
        git 'https://github.com/kamkaz12/Calc.git'
    }

    stage('Create Nuget Restore'){
        build job: 'NugetRestore', parameters: [string(name: 'path', value: ''${WORKSPACE}\\calc.sln')]build job: 'NugetRestore', parameters: [string(name: 'path', value: ''${WORKSPACE}\\calc.sln')]
    }
}